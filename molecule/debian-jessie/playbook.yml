---
- name: Converge
  hosts: all
  gather_facts: no

  roles:
    - role: robertdebock.bootstrap
    - role: ansible-role-nginx

  tasks:
    - name: test nginx
      shell: nginx -V > nginx.version
      args:
        creates: nginx.version
