---
- hosts: all
  remote_user: root

  roles:
    - ansible-role-nginx

  tasks:
    - name: test if port is open
      wait_for:
        host: "{{ inventory_hostname }}"
        port: "{{ appport }}"
        delay: 5
        timeout: 30
