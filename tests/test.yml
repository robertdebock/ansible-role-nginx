---
- hosts: localhost
  remote_user: root

  roles:
    - ansible-role-nginx

  tasks:
    - name: test if port is open
      wait_for:
        host: localhost
        port: "{{ port }}"
        delay: 5
        timeout: 30

